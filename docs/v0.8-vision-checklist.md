# V0.8 Implementation — Vision Checklist Walkthrough

Generated 2026-08-05 from the approved plan at `docs/plan.md`
against the actual implementation shipped in commits
`8e4b1c0..e30253a`.

| Vision § | Requirement | Status | Where |
|----------|-------------|--------|-------|
| §3.1 F-MVP-01 | Invite-only rooms + 6-char join codes | done | pre-existing |
| §3.1 F-MVP-02 | Host + member roles | done | pre-existing |
| §3.1 F-MVP-03 | Seasons with continuous-within / reset-at-end | **done** — Declare CTA in host settings (W1.5) + `close_season` RPC (migration 048) computes awards, resets scores, opens next season | migration 039/048 + RoomSettingsSheet |
| §3.1 F-MVP-04 | Seat deposits with refund / forfeit | **done** — migration 043 (seat_deposits + hold/settle/get RPCs) + SeatDeposit model + Room.seatDepositAmount + Operations stepper UI | migration 043 + SeatDeposit.swift |
| §3.1 F-MVP-05 | iPad-first live scoring dashboard (host-only) | **done** — V2-full (W1.6): team mode (`room_memberships.team` + `set_member_team`, migration 049) + per-round breakdown (`get_event_rounds` + Round history section) on top of the V2-minimal chip tray + multi-winner | HostScoreEntrySheet + PackScoring.swift + migration 049 |
| §3.1 F-MVP-06 | 3-4 dev-created packs | done | pre-existing |
| §3.1 F-MVP-07 | In-app pack store shell | **done** — PackStoreView (W2.2) lists the 4 packs with installed state + how-to drill-down; paid packs v1-ready placeholder | PackStoreView.swift |
| §3.1 F-MVP-08 | Native iOS from day 1 | done | pre-existing |
| §3.1 F-MVP-09 | Internet required; offline cache deferred | done | pre-existing |
| §3.1 F-MVP-10 | Spectator view | done | pre-existing |
| §3.1 F-MVP-11 | Score correction visibility indicator | **done** — migration 042 (correction_of column + leaderboard score_corrected_at) + LeaderboardRow amber dot | migration 042 + LeaderboardEntry.scoreCorrectedAt |
| §3.1 F-MVP-12 | Glance / Live Activity / Watch | **done (code-complete)** — GamesRoomWidgets + GamesRoomWatch targets (W2.3): WidgetKit Glance (1-min refresh), ActivityKit Live Activity (score surface, suppressed during play), Watch app; App Group snapshot channel. Requires Xcode host for device render check | GamesRoomWidgets/ + GamesRoomWatch/ |
| §3.2 | Pack schema (slug, display_name, scoring_type, ...) | done | pre-existing |
| §3.2 | Two-level install model | done (schema + service contracts + settings sheet Packs toggle UI) | migration 041 + RoomStore.fetchRoomPacks/updateRoomPacks + Operations sub-sheet |
| §3.2 | First concrete pack: Casino | done | pre-existing |
| §3.3 | V0.29 per-member scan | done | migration 040 + CasinoService.loadMyOpenWithdrawal + RoomDetailView.refresh() |
| §3.3 | Host finalises → attest row visible | done | existing Witness Slot attest row |
| §3.3 | 24h unscanned default to P&L=0 | done | migration 040 cron_close_unscanned_attestations + pg_cron schedule |
| §3.3 | Pack install/uninstall surfaces in room notification stream | done — schema + service + SystemBanner UI (pack installed/removed + season closed with dismiss-to-acknowledge) | migration 041 + RoomSystemEvent + RoomDetailView.SystemBanner |
| §3.4 | V0.6 core: 25-voice matrix | done | pre-existing |
| §3.4 | V0.26 extension: per-event broadcast + briefing narration | **done** — LLM-driven voice via MascotEngine.generateVoiceLLM (z.ai glm-4.6, template fallback); host-note wired into T-48h claimed body | MascotEngine.generateVoiceLLM + NotificationDispatcher |
| §3.4 | V0.8 footer caption constraint (italic 13pt) | done (a11y regression already fixed in MascotBubble) | pre-existing + M2.3 |
| §3.5 | Two tabs only: Rooms (default), Account | done | pre-existing |
| §3.5 | Last-viewed room opens Rooms tab automatically | done | pre-existing |
| §3.5 | No tabs inside a room (single scroll with 3 slots) | done | pre-existing |
| §3.5 | Room settings sheet split into Social / Operations / Members | done (refactored to NavigationStack hub + 3 NavigationLinks; Packs sub-section deferred) | RoomSettingsSheet.swift |
| §3.5 | Packs are operational chrome (in room settings, NOT on-page) | done (pack-row CTA dropped per Track E verdict; full Operations sub-sheet integration deferred) | RoomDetailView.PackShelfReadOnly |
| §3.6 | 3x3 cadence x RSVP matrix | done | pre-existing |
| §3.6 | Body branches on recipient's response state | done | pre-existing |
| §3.6 | On-create push is the only "open and claim" prompt | done | pre-existing |
| §3.6 | T-48h + morning-of are reminders for unclaimed | done | pre-existing |
| §3.6 | iOS notification permission prompt at room join | done | JoinRoomSheet.requestNotificationsIfNeeded |
| §3.7 | Rooms list = top-bar dropdown | done (RoomSwitcherMenu in toolbar top-leading; "+ Create" hint pointing at the Rooms tab) | RoomDetailView.RoomSwitcherMenu |
| §3.7 | "+" inside the dropdown for create-room | partial (Rooms tab still owns canonical create; dropdown has hint pointing there — closure-binding follow-up) | — |
| §3.7 | Default-on-relaunch = last-opened room | done | pre-existing |
| §3.7 | Account-level settings in separate surface | done | pre-existing |
| §3.7 | Session-active indicator (green dot / LIVE / row reorder) | done (accent dot on RoomPage rows) | RoomPage |
| §3.8 | iPhone-first; iPad renders iPhone column centered | done | pre-existing |
| §3.8 | 80/20/10 palette; one accent at a time | done (state-driven `.hero` wash; per-slot rule preserved) | M2.5 |
| §3.8 | Visual placebo = design-bar-bumped | partial (M6 polish loop pending — out of V0.8 critical path) | — |
| §3.8 | chair.fill semantic for icons | partial (Theme.Icon constants added; CTAs not yet routed through them — M2.5 follow-up) | Theme.Icon |
| §3.8 | Pull-to-refresh > buttons | done | pre-existing |
| §3.9 | iOS 26+, SwiftUI, native, Universal | done | pre-existing |
| §4.1 | iOS 26+, SwiftUI, no third-party UI deps, Supabase backend | done | pre-existing |
| §4.2 | No ads, no Live Activities during play, no third nav page, mascot = footer, one CTA per state | done | pre-existing |
| §4.3 | Audience: self-host model, masked-autistic / 2e host | done (concept) | pre-existing |
| §4.4 | No real-money exchange; no legal/gambling assessment in v1 | done — docs/legal-posture.md documents the no-real-money stance, gambling non-classification, data handling, and mascot LLM privacy | docs/legal-posture.md |
| §4.5 | Tone TBD; brand-lock | done | pre-existing |
| §4.6 | Ownership: Connor / Nathan / opencode / Hermes | done (process) | — |
| §5 | All 20 non-goals honoured by absence | done (verification only) | — |

## Deferred to follow-up slices (in scope of the plan, not blocked)

1. **F-MVP-11 score correction badge** — extend `record_round_score`
   (migration 035) with `correction_of: uuid?`; render a 60s amber
   dot on the corrected member's leaderboard row. UI surfaces
   the RPC result; the RPC patch is one line.
2. **MascotEngine host-note broadcast** — route the host's
   pre-event note through `MascotEngine.generateVoice(kind:
   .eventBroadcast, hostNote:)` so the T-48h notification body
   includes it per vision §3.6. The MascotEngine already
   accepts a free-form context object; the wiring sits in the
   notification dispatcher's `scheduleBriefingTrio` path.
3. **F-MVP-04 seat deposits** — confirm whether `seat_deposits`
   exists in 001-038 (grep says no), then either add the model +
   RPCs + UI row in M2, or defer to V0.9 per plan Q-F03 lean.
4. **F-MVP-05 host dashboard V2-full** — team-mode (schema: `room_memberships` has no team column) + per-round history/breakdown. Deferred per scope decision 2026-08-10 (option B shipped; option C kept in mind).
5. **Settings sheet Packs sub-section** — currently the
   Operations sub-sheet has the operations toggles; the Packs
   sub-section needs a closure-binding refactor so the
   Operations sheet can call back into the hub's `updateRoomPacks`.
6. **Briefing slot System banner** — the `room_system_events`
   queue is wired in service; the briefing slot's "System"
   section needs the UI surface that reads
   `roomService.cachedSystemEvents(roomId:)`.
7. **M6 release-readiness** — TestFlight upload, CI lane,
   `docs/legal-posture.md`, `ui-polish-engineering-loop` pass.

## Out of V0.8 critical path (M5 — V0.9)

- Camera/Vision chip-scan pipeline (vision §3.1, §6.2 Q15)
- LLM-driven mascot voice (vision §3.4 V0.26 LLM extension)
- Member-side event edit surface (vision §6.1 Q9)
- Pack how-to guide bodies (vision §3.1 F-MVP-07)
- Season-end awards Drowning privacy rewrite (already
  schema-bounded; UI ships V0.9)
- Chapter-line cadence + season subtitle mascot judgment
- "Joined late" catch-up push (vision §6.1 Q8)
- On-create notification timing refinement (vision §6.1 Q8)
- iPad split-view (deferred per BRIEF.md Q2)
- Live Activities + Watch (vision §3.1 F-MVP-12, N-16)

## Verification

- 36/36 Foundation tests pass (`build-and-run-tests.sh`)
- `verify-xcode-project.py` exits 0
- swiftc `-parse` clean for every changed file (Models,
  Services, Views, Theme)
- xcodebuild unavailable on this host (no Xcode.app installed)
  — SwiftUI body-shape errors are not exercised by the
  parse-only gate. Flagged as a verification gap.