# V0.41 — Data-loading optimisation loop

## Task
Implement the data-loading optimisation pass for GamesRoom. Read
`.hermes/prompts/V0.41_optimization_SPEC.md` for the full spec.

## Instructions
1. Read `.hermes/prompts/V0.41_optimization_SPEC.md`.
2. Apply the four changes exactly as specified:
   - Change 1: Add cache TTL (30s) + `force:` param to all 14 load methods in
     `GamesRoom/Services/RoomService.swift`, plus TTL invalidation on writes.
   - Change 2: Parallelise the post-claim reloads in `upsertEventRSVP`.
   - Change 3: Add `applyOptimisticRSVP` to RoomService and wire it into
     `claimSeat` / `declineSeat` / `releaseSeat` in RoomDetailView.
   - Change 4: Thread `force:` through `RoomDetailView.refresh()` and the
     `loadXIfNeeded` helpers; force on pull-to-refresh + sheet onDone.
3. Scope strictly to `GamesRoom/Services/RoomService.swift` and
   `GamesRoom/Views/RoomDetailView.swift`. Do NOT touch any other file.
4. Do NOT add comments unless the existing code style requires it.
5. Do NOT run git commit/add/revert. Leave the working tree with only your
   edits uncommitted.

## Verification
Run from the repo root:
```
./build-and-run-tests.sh
```
The output must end with "Ran 146 cases: 146 passed, 0 failed." and
"parse-check-swiftui: pass=42 fail=0". If the parse-check fails, fix the
Swift syntax and re-run until it passes.

Then run `git status --short` and confirm the ONLY modified files are
`GamesRoom/Services/RoomService.swift` and `GamesRoom/Views/RoomDetailView.swift`.
Report the final test output and the git status.
