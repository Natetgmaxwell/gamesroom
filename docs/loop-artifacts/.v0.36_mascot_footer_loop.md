# V0.36 mascot-footer loop (self-contained)

Read `V0.36_MASCOT_FOOTER_SPEC.md` in this directory first — it is the
authoritative spec; this file is the loop contract.

Repo: `/Users/nathanmaxwell/Documents/VS Code/games-room` (workdir). Branch:
main. Do NOT create a branch.

## Task

Make the mascot footer caption on the room page dynamic: it must be
self-aware of the room's state (no events / active session / post-play /
stale >2 weeks / between events) and reference real data (event title,
recent winners, standings leader, caller rank, days quiet). Personality ×
ideology still shapes tone via the 25-voice matrix.

## Hard constraints

1. **Exactly six files may change:**
   - `GamesRoom/Services/MascotEngine.swift`
   - `GamesRoom/Views/RoomDetailView.swift`
   - `main.swift`
   - `build-and-run-tests.sh`
   - `docs/loop-artifacts/V0.36_MASCOT_FOOTER_SPEC.md` (only if a
     deviation must be recorded)
   - `docs/loop-artifacts/.v0.36_mascot_footer_loop.md` (this file)
   Do NOT touch: Theme.swift, pbxproj, any migration, any other Swift
   file. The working tree has sibling WIP (V0.35 season-history is
   mid-flight in this same repo) — never `git add -A`, never commit
   anything outside this list.
2. **No new Swift files.** No pbxproj change. No migration (pure
   client-side).
3. **TDD where possible.** The Foundation runner covers Models + Packs +
   (new) MascotEngine. Add MascotEngine.swift to `SRC_FILES` in
   build-and-run-tests.sh, then write tests in main.swift per the spec:
   footerKind resolution (all 5 states), sentence-drop nil-safety,
   recentWinners/leaderName/callerRank derivations, {member_count}
   substitution, winner-sentence append. Do not add tests for SwiftUI
   view logic (the runner can't cover it).
4. **Theme tokens only.** No new tokens, no hardcoded colors/fonts/sizes.
5. **No dead code, no TODOs, no comments that restate the code.** Match
   each file's existing header/MARK/doc-comment conventions.
6. **Backward-compatible RoomContext.** New fields default (nil/[]), so
   `NotificationDispatcher.swift`'s 4-arg construction compiles unchanged.
7. **Existing NotificationKind call sites keep compiling.** The 4 new
   cases are additive; `buildLLMPrompt`'s switch gains the new branches.
8. **Sentence-drop must not regress existing callers.** Existing
   placeholders ({time}, {venue}, {seats_left}, {seats_claimed},
   {host_note}, {date}) keep their current "" substitution; only the NEW
   placeholders ({winner}, {leader}, {caller_rank}, {event_count},
   {days_quiet}, {member_count}) and {event} leave the token in place for
   the sentence-drop pass.

## Deliverables

1. MascotEngine: 4 new NotificationKind cases (roomWelcome, inPlay,
   roomStale, standings); RoomContext +3 fields with defaults;
   `footerKind(activeEvent:leaderboard:now:)`; `recentWinners`,
   `leaderName`, `callerRank` derivations; sentence-drop pass in
   `interpolate`; {member_count} substitution; 100 new matrix cells
   (4 kinds × 25) + winner sentences appended to the 25 postPlayRecap
   cells; buildLLMPrompt additions.
2. RoomDetailView: `MascotFooterCaption` takes `activeEvent`,
   `leaderboard`, `currentUserId`; call site (~line 360) passes them;
   context built per spec §6.
3. main.swift: tests per spec §3.
4. build-and-run-tests.sh: MascotEngine.swift added to SRC_FILES.

## Verification (mandatory, in order)

1. `./build-and-run-tests.sh` from repo root — all green. Report the new
   test count (87 will shift).
2. `./scripts/parse-check-swiftui.sh` — green.
3. `git diff --stat` — exactly the six files above.
4. Commit (one logical change):
   `feat: mascot footer caption driven by room state (V0.36)`
5. Report: files changed, test count, parse-check result, deviations.

## Sequencing note (sibling WIP)

V0.35 season-history is mid-flight in this repo (opencode PID 66930,
editing RoomDetailView.swift + main.swift). If the sibling is still
running when this loop starts, do the collision-free files first
(MascotEngine.swift, build-and-run-tests.sh, spec/loop files), then wait
for the sibling to commit before touching RoomDetailView.swift and
main.swift. Re-run the baseline test suite after the sibling lands; if
their work is incomplete (e.g. main.swift references removed symbols),
do NOT fix their code — flag it in the completion report and proceed
with only the parts of main.swift that compile.
