# V0.50 vision-first settle loop (self-contained)

Read `V0.50_VISION_FIRST_SETTLE_SPEC.md` in this directory first — it is the
authoritative spec; this file is the loop contract.

Repo: `/Users/nathanmaxwell/Documents/VS Code/games-room` (workdir). Branch: main.
Do NOT create a branch. Do NOT commit.

## Task

Unify the settle/score surface so vision is the PRIMARY CTA for both packs
(casino + CAH) and both roles (host + member). Hosts currently never reach the
vision scan sheets. Manual/aggregate entry becomes a secondary link one tap
behind.

## Hard constraints

1. **Exactly these files may change:**
   - `GamesRoom/Views/RoomDetailView.swift`
   - `docs/loop-artifacts/V0.50_VISION_FIRST_SETTLE_SPEC.md` (only if a deviation
     must be recorded)
   - `docs/loop-artifacts/.v0.50_vision_first_settle_loop.md` (this file)
   Do NOT touch: pbxproj, any migration, ChipScanSheet.swift, CAHCardScanSheet.swift,
   SettleCasinoSheet.swift, HostScoreEntrySheet.swift, main.swift, any other file.
   There are untracked `.designs/` and `build/` dirs — never `git add -A`, never
   commit anything outside this list.
2. **No new Swift files.** No pbxproj change. No migration.
3. **WitnessSlot** is a private struct inside RoomDetailView.swift — edit it in
   place (do not extract to a Components/ file).
4. **Vision-first for hosts.** The `settleSheetEvent` sheet binding currently has
   an `if isHost { SettleCasinoSheet } else { ChipScanSheet }` branch (~line
   288-313). Remove the role branch: this binding ALWAYS presents `ChipScanSheet`.
5. **`openHostScore(event:)`** (~line 988-997) must branch on `scoringType`:
   - `.withdrawReturn` (casino) → `settleSheetEvent = event` (ChipScanSheet).
   - `.countBased` (CAH) → `cahScanEvent = event` (CAHCardScanSheet).
   - default (`single_winner`) → unchanged: iPad regular → `scoringDashboardVisible`,
     else `hostScoreEvent`. CAH must NOT route to the dashboard (it only renders
     `single_winner`).
6. **Host aggregate/manual links** (one tap behind vision):
   - New `@State private var hostManualSettleSheetEvent: Event?` + `.sheet(item:)`
     presenting `SettleCasinoSheet(isHost: true)` for casino hosts.
   - CAH host manual entry reuses the existing `hostScoreEvent` →
     `HostScoreEntrySheet`.
   - New `hostManualSettleAction(for:)` helper: casino host → flips
     `hostManualSettleSheetEvent`; CAH host → flips `hostScoreEvent`; nil otherwise.
7. **WitnessSlot** gains an `onHostManualSettle` callback (host-only, rendered as
   a secondary link below "Score a round", never a second filled primary). The
   existing `onManualSettle` stays member-only. One filled primary (vision) per
   slot; manual/aggregate are links.
8. Match each file's existing conventions (Theme.Typography, Theme.Palette,
   `.pressScale()`, Theme.Icon). No dead code, no TODOs, no comments that restate
   the code.

## Deliverables

1. Casino member settle → ChipScanSheet; manual one tap behind. (confirm unchanged)
2. Casino host "Score a round" → ChipScanSheet; aggregate settle one tap behind.
3. CAH host "Score a round" → CAHCardScanSheet; manual entry one tap behind.
4. Working hand badge (already wired via `casinoWithdrawn`, populated by the
   t_350c0cd7 fix) renders for host and member in `.inPlay` and `.settleRound`.
5. No second filled primary button in any slot.

## Verification (mandatory, in order)

1. `./build-and-run-tests.sh` from repo root — all green. Report the test count.
2. `./scripts/parse-check-swiftui.sh` — green.
3. `python3 scripts/verify-xcode-project.py` — green (pbxproj untouched).
4. `git diff --stat` — exactly the files above.
5. Do NOT commit. Report: files changed, test count, parse-check result,
   verify-xcode-project result, any deviation from the SPEC.
