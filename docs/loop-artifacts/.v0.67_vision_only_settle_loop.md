# V0.67 vision-only settle loop (self-contained)

Read `docs/loop-artifacts/V0.67_VISION_ONLY_SETTLE_SPEC.md` in this directory — it is
the authoritative spec; this file is the loop contract.

Repo: `/Users/nathanmaxwell/Documents/VS Code/games-room` (workdir). Branch: main.
Do NOT create a branch. Do NOT commit (orchestrator commits).

## Task

Remove every manual-settle surface. Vision becomes the ONLY settle path. Delete
`SettleCasinoSheet.swift` and its 4 pbxproj entries. Keep the in-scan
confirm/adjust steppers (ChipScanSheet + CAHCardScanSheet) and keep
`HostScoreEntrySheet` for `.singleWinner` packs.

## Current code facts (verified — do not re-verify)

- `GamesRoom/Views/RoomDetailView.swift`:
  - `@State private var manualSettleSheetEvent: Event?` (line 87) — flips
    `SettleCasinoSheet(isHost:false)` via `.sheet(item:)` at line 366.
  - `@State private var hostManualSettleSheetEvent: Event?` (line 93) — flips
    `SettleCasinoSheet(isHost:true)` via `.sheet(item:)` at line 383.
  - `private func manualSettleAction(for event: Event) -> (() -> Void)?` (line 1173).
  - `private func hostManualSettleAction(for event: Event) -> (() -> Void)?` (line 1218).
  - `private struct WitnessSlot: View` (line ~1690): stored props `onManualSettle`
    (line 1766), `onHostManualSettle` (line 1772). Body renders member manual link
    inside `case .scan:` (line 1855-1872) and host manual link inside
    `if let onScore` (line 1916-1935).
  - WitnessSlot call sites: `.inPlay` (line 716) passes `onManualSettle` (line 732)
    + `onHostManualSettle` (line 736); `.settleRound` (line 791) passes
    `onManualSettle` (line 809) + `onHostManualSettle` (line 813); `.tonightEvent`
    (line 751) passes neither.
- `GamesRoom/Views/SettleCasinoSheet.swift` — the manual sheet. After the
  RoomDetailView removals it has zero call sites (verify with grep before deleting).
- `GamesRoom.xcodeproj/project.pbxproj` — 4 entries for SettleCasinoSheet.swift:
  PBXBuildFile `AAAAAAAA00000000000000E9` (line 91), PBXFileReference
  `AAAAAAAA0000000000000441` (line 223), group child (line 424), Sources phase
  entry (line 721).
- Comment mentions of `SettleCasinoSheet` to clean up:
  - `GamesRoom/Views/HostScoringDashboard.swift:27`
  - `GamesRoom/Views/HostScoreEntrySheet.swift:40`
  - `GamesRoom/Services/CasinoService.swift:459` (doc comment on
    `loadMyOpenWithdrawal`)

## Changes (apply exactly)

### 1. RoomDetailView.swift — remove manual settle surfaces

- Remove `@State private var manualSettleSheetEvent: Event?` (line 87) and its
  doc comment block.
- Remove `@State private var hostManualSettleSheetEvent: Event?` (line 93) and its
  doc comment block.
- Remove the `.sheet(item: $manualSettleSheetEvent)` presentation (line 366-376).
- Remove the `.sheet(item: $hostManualSettleSheetEvent)` presentation (line 383-393).
- Remove `private func manualSettleAction(for:)` (line 1173-1176).
- Remove `private func hostManualSettleAction(for:)` (line 1218-1227).
- In `WitnessSlot` struct: remove `var onManualSettle` (line 1766) + doc comment,
  and `var onHostManualSettle` (line 1772) + doc comment.
- In `WitnessSlot.body`: remove the `if let onManualSettle { ... }` block inside
  `case .scan:` (line 1855-1872). Remove the `if let onHostManualSettle { ... }`
  block inside `if let onScore { ... }` (line 1916-1935).
- At `.inPlay` call site: remove `onManualSettle: manualSettleAction(for: event),`
  (line 732) and `onHostManualSettle: hostManualSettleAction(for: event),` (line 736).
- At `.settleRound` call site: remove `onManualSettle: manualSettleAction(for: event),`
  (line 809) and `onHostManualSettle: hostManualSettleAction(for: event),` (line 813).

### 2. Delete SettleCasinoSheet.swift + pbxproj entries

- `rm GamesRoom/Views/SettleCasinoSheet.swift` (after grep confirms zero call sites).
- Remove the 4 pbxproj entries (lines 91, 223, 424, 721).
- Run `python3 scripts/verify-xcode-project.py`.

### 3. Comment cleanup

- `HostScoringDashboard.swift:27` — remove/reword the `SettleCasinoSheet` mention.
- `HostScoreEntrySheet.swift:40` — remove/reword the `SettleCasinoSheet` mention.
- `CasinoService.swift:459` — remove/reword the `SettleCasinoSheet` mention in the
  `loadMyOpenWithdrawal` doc comment.

## Do NOT touch

- ChipScanSheet.swift, CAHCardScanSheet.swift, HostScoreEntrySheet.swift (logic),
  any migration, main.swift, any other file.
- The pre-existing uncommitted V0.62 TEMP verification hooks in ContentView.swift /
  RoomPage.swift / RoomDetailView.swift (the `loadWorkingHandsIfNeeded` guard).
  Leave them in the working tree; do NOT stage or commit them.

## Verification (mandatory, in order)

1. `grep -r "SettleCasinoSheet\|Settle manually" GamesRoom/` returns nothing.
2. `./build-and-run-tests.sh` from repo root — all green. Report test count and
   parse-check file count.
3. `python3 scripts/verify-xcode-project.py` — green.
4. `git diff --stat` — only the allowed files.
5. `git diff` review — confirm: no manual settle link remains in WitnessSlot,
   no SettleCasinoSheet reference remains, the vision CTAs (ChipScanSheet /
   CAHCardScanSheet) are untouched, HostScoreEntrySheet logic untouched.

## Allowed files

- `GamesRoom/Views/RoomDetailView.swift`
- `GamesRoom/Views/SettleCasinoSheet.swift` (deleted)
- `GamesRoom/Views/HostScoringDashboard.swift` (comment only)
- `GamesRoom/Views/HostScoreEntrySheet.swift` (comment only)
- `GamesRoom/Services/CasinoService.swift` (comment only)
- `GamesRoom.xcodeproj/project.pbxproj`
- `docs/loop-artifacts/V0.67_VISION_ONLY_SETTLE_SPEC.md` (this spec)
- `docs/loop-artifacts/.v0.67_vision_only_settle_loop.md` (this contract)
Do NOT touch anything else. Never `git add -A`.
