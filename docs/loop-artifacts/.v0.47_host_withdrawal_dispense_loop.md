# V0.47 host-withdrawal-dispense loop (self-contained)

Read `V0.47_WITHDRAWAL_DISPENSE_SPEC.md` in this directory first — it is the
authoritative spec; this file is the loop contract.

Repo: `/Users/nathanmaxwell/Documents/VS Code/games-room` (workdir). Branch:
main. Do NOT create a branch.

## Task

Add a host-only "Chips to dispense" surface so the host sees every member
casino withdrawal (name + amount) and can tap "Dispensed" to clear it from a
pending list (local state only — the ledger is never mutated). Harden the
member balance re-read with pull-to-refresh on WithdrawChipsSheet. The server
already guards the insufficient-balance path (23514); the sheet already shows
it as an errorMessage — keep that.

## Hard constraints

1. **Exactly these files may change:**
   - `GamesRoom/Views/RoomDetailView.swift` (host section + state + loader +
     `refresh()` wiring + new private `HostWithdrawalsSection`)
   - `GamesRoom/Views/WithdrawChipsSheet.swift` (add `.refreshable` only)
   - `docs/loop-artifacts/V0.47_WITHDRAWAL_DISPENSE_SPEC.md`
   - `docs/loop-artifacts/.v0.47_host_withdrawal_dispense_loop.md` (this file)
   Do NOT touch: pbxproj, any migration, Theme.swift (no new icon needed —
   reuse `Theme.Icon.circleHexagongridFill`), HostScoringDashboard.swift,
   CasinoService.swift, main.swift, any other file. There are untracked
   `.designs/` and `build/` dirs — never `git add -A`, never commit anything
   outside this list.
2. **No new Swift files.** No pbxproj change. No migration.
3. **`HostWithdrawalsSection`** is a NEW private struct inside
   RoomDetailView.swift — edit it in place, do not extract to a Components/ file.
4. **Casino is `withdrawReturn` scoring**, so HostScoringDashboard (gated to
   `.singleWinner`) NEVER renders for casino — the host board MUST go in
   RoomDetailView's `scrollBody`, host-only, right after `activeSlot`.
5. **`casino_withdrawal` rows have negative `amount_points`** — display the
   absolute value (`abs(txn.amountPoints)`).
6. Filter `getEventTransactions` results to `kind == "casino_withdrawal"` only.
   Do NOT add a new RPC.
7. Hide the section entirely when there are no pending withdrawals.
8. Match each file's existing conventions (Theme.Typography, Theme.Palette,
   `.sectionCard(.standard)`, Theme.Icon). No dead code, no TODOs, no comments
   that restate the code.

## Deliverables

1. Host opens the active casino event → sees a "Chips to dispense" section
   listing every withdrawal (member name + absolute amount) with a "Dispensed"
   button.
2. Tapping "Dispensed" clears that row from the pending list (local `@State`
   only). Ledger untouched.
3. Section hidden when no pending withdrawals remain.
4. WithdrawChipsSheet Form gets `.refreshable { await loadBalance() }` so the
   member can re-read their balance. Everything else in the sheet unchanged.

## Verification (mandatory, in order)

1. `./build-and-run-tests.sh` from repo root — all green. Report the test
   count.
2. `./scripts/parse-check-swiftui.sh` — green.
3. `./scripts/verify-xcode-project.py` — green (pbxproj untouched).
4. `git diff --stat` — exactly the files above.
5. Do NOT commit. Report: files changed, test count, parse-check result,
   verify-xcode-project result, any deviation.
