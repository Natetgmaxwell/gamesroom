# V0.38 mascot-voice loop (self-contained)

Read `V0.38_MASCOT_VOICE_SPEC.md` in this directory first — it is the
authoritative spec; this file is the loop contract.

Repo: `/Users/nathanmaxwell/Documents/VS Code/games-room` (workdir). Branch:
main. Do NOT create a branch.

## Task

Rewrite the mascot voice so it reads like a wry friend, not a form letter:
replace all 200 template-matrix cells per the spec's pinned matrix, make the
five personalities FEEL different on the same fact, calibrate tone
(informational, light, never dramatic — no ALL-CAPS, ≤1 `!`, ≤200 chars),
and move the legacy logistics placeholders into the nil-preserving
sentence-drop set so the footer never renders "at .".

## Hard constraints

1. **Exactly five files may change:**
   - `GamesRoom/Services/MascotEngine.swift`
   - `main.swift`
   - `docs/loop-artifacts/V0.38_MASCOT_VOICE_SPEC.md` (only if a
     deviation must be recorded)
   - `docs/loop-artifacts/.v0.38_mascot_voice_loop.md` (this file)
   - `docs/loop-artifacts/.v0.38_mascot_voice_prompt.md` (this dispatch's
     driver prompt)
   Do NOT touch: RoomDetailView.swift, NotificationDispatcher.swift,
   Theme.swift, pbxproj, any migration, any other Swift file. The working
   tree has untracked `.designs/app-icons-v4/` — never `git add -A`, never
   commit anything outside this list.
2. **No new Swift files.** No pbxproj change. No migration (pure
   client-side).
3. **Transcribe the matrix cells VERBATIM from spec §6.** The voice design
   is pinned; do not re-word cells. If a cell cannot be transcribed exactly,
   stop and record the deviation in the spec's Decisions section instead of
   improvising.
4. **Placeholder contract:** the nil-preserving + sentence-drop set covers
   exactly {event}, {winner}, {leader}, {caller_rank}, {event_count},
   {days_quiet}, {time}, {venue}, {seats_left}, {seats_claimed}.
   {mascot}, {room}, {member_count} stay always-substituted. {date} and
   {host_note} keep `""` substitution. Implement this by extending the
   existing `if let` substitution chain in `interpolate` to cover the four
   legacy logistics placeholders; do NOT change the sentence-drop pass
   itself (it already handles any placeholder left in place).
5. **systemPrompt:** change "1-2 sentences" to "1-3 short sentences" and
   append: "Tone is informational and light — a quiet footer caption, never
   dramatic. No ALL-CAPS. At most one exclamation mark."
6. **Header comment:** update the matrix-size reference (the header's
   "5 × 5 × 4 = 100" style counts are stale — 8 kinds now), the sentence
   guidance (1–3 sentences, ≤200 chars), and the legacy-placeholder
   paragraph (superseded: logistics placeholders are now nil-preserving).
   Match the file's existing doc-comment conventions.
7. **Tests:** update the six re-pinned assertions per spec's Test plan
   table; add the six new tests per spec. Use the `runner` harness already
   in main.swift. `MascotPersonality` and `MascotPoliticalIdeology` are
   both `CaseIterable` — loop them for the 200-cell tests.
8. Match each file's existing conventions. No dead code, no TODOs, no
   comments that restate the code.

## Deliverables

1. MascotEngine.swift: 200 cells replaced verbatim; legacy logistics
   placeholders joined the nil-preserving set; systemPrompt tone line;
   header comment updated.
2. main.swift: re-pinned assertions + 6 new tests (200-cell render,
   ≤200 chars, 25-distinct-per-kind, unhinged-quiet, no `(s)`, legacy
   sentence drop).
3. The two spec/loop docs (this file + the spec, only if deviation).

## Verification (mandatory, in order)

1. `./build-and-run-tests.sh` from repo root — all green. The 111 existing
   cases shift: report the new count.
2. `./scripts/parse-check-swiftui.sh` — green (42 files).
3. `git diff --stat` — exactly the five files above.
4. Do NOT commit. Report: files changed, test count, parse-check result,
   any deviation from the pinned matrix.
