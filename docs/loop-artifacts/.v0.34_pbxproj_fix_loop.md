# V0.34 pbxproj fix — register SessionRoundTimeline.swift in the Sources build phase

One-line additive fix in `GamesRoom.xcodeproj/project.pbxproj`.

## Problem

The previous drive added three pbxproj lines for the new file
`GamesRoom/Views/Components/SessionRoundTimeline.swift`:
- PBXBuildFile `AAAAAAAA0000000000000482` (line ~102)
- PBXFileReference `AAAAAAAA0000000000000481` (line ~241)
- group-children entry `AAAAAAAA0000000000000481` (line ~418)

But the `AAAAAAAA0000000000000482` build-file reference is MISSING from the
GamesRoom target's `PBXSourcesBuildPhase` — the file is registered but never
compiled into the target.

## Fix

Insert exactly one line into the GamesRoom target's Sources build phase, in the
Components group block, immediately after the DrowningBadge entry:

```
				AAAAAAAA0000000000000447 /* DrowningBadge.swift in Sources */,
```

becomes

```
				AAAAAAAA0000000000000447 /* DrowningBadge.swift in Sources */,
				AAAAAAAA0000000000000482 /* SessionRoundTimeline.swift in Sources */,
```

Match the existing indentation (4 tabs) and the trailing comma. Do NOT touch
any other line in the file. Do NOT modify any other file.

## Verification

1. `grep -n "0482" GamesRoom.xcodeproj/project.pbxproj` — must show the
   PBXBuildFile definition (line ~102) AND the Sources-phase reference.
2. `awk '/Begin PBXSourcesBuildPhase section/,/End PBXSourcesBuildPhase section/' GamesRoom.xcodeproj/project.pbxproj | grep 0482` — must match.
3. `git diff GamesRoom.xcodeproj/project.pbxproj` — exactly 4 added lines total,
   all additive, no deletions.
