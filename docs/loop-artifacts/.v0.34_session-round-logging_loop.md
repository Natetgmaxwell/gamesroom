# V0.34 session-round-logging loop (self-contained)

You are implementing the in-session round logging core in the Games Room repo.
Work in the current directory (repo root). Read `V0.34_SESSION_ROUND_LOGGING_SPEC.md`
in this directory first — it is the authoritative spec; this file is the loop
contract.

## Task

Build the dependency-independent core for kanban task t_58f0ff13 (in-session
game logging): round auto-increment, round delete/undo, the correction
double-count fix, and the session timeline component. The iPad dashboard
surface is a sibling task — do NOT build dashboard layout or per-pack input
forms.

## Hard constraints

1. **Sibling files are dirty and owned by a parallel worker — DO NOT TOUCH:**
   `GamesRoom.xcodeproj/project.pbxproj` (except the additive registration of
   the new view file, §4 of the spec), `GamesRoom/Services/CasinoService.swift`,
   `GamesRoom/Views/RoomDetailView.swift`, `GamesRoom/Views/RoomSettingsSheet.swift`,
   `GamesRoomWidgets/Info.plist`. Never modify an existing pbxproj line.
2. **TDD.** Add the five test cases to `main.swift` (before `// MARK: - Summary`)
   FIRST, watch them fail, then implement. The Foundation runner compiles
   Models/ + Packs/ + StorageKeys + RoomStoreProtocol + InMemoryRoomStore.
3. **Theme tokens only.** `Theme.Palette`, `Theme.Typography`, `Theme.Layout`,
   `Theme.Icon`. No new tokens, no hardcoded colors/fonts/sizes. One accent
   (brass). Negative deltas render muted (`primaryText` opacity ~0.6), never red.
4. **No dead code, no TODOs, no comments that restate the code.** Match the
   existing file conventions (header comment blocks, MARK sections, doc
   comments on public API).
5. **Migration discipline.** One file
   `Supabase/migrations/054_round_delete_and_correction_fix.sql` with the three
   changes from spec §1. `record_round_score` fix uses `create or replace`
   (signature unchanged); `get_event_rounds` needs DROP + CREATE (RETURNS TABLE
   column list changes — Postgres error 42P13). Grant execute on every function.
6. **pbxproj registration is additive only** — new PBXBuildFile +
   PBXFileReference + group-children line + Sources-phase line, next sequential
   `AAAAAAAA00000000000004XX` ids, verify no id collision.

## Deliverables (from the spec)

1. Migration 054: correction double-count fix in `record_round_score` (reverse
   old deltas from `season_score` before applying new), new
   `delete_round_score(p_room_id, p_event_id, p_round_index)` RPC (host-only,
   idempotent, reverses deltas + deletes transactions + deletes the row),
   `get_event_rounds` extended with `correction_of uuid`.
2. `GamesRoom/Models/EventRound.swift`: add `correctionOf: UUID?` (CodingKey
   `correction_of`, decodeIfPresent, default nil) + `Array<EventRound>.nextRoundIndex`
   extension (max+1, monotonic).
3. `GamesRoom/Services/ScoringService.swift`: `ScoringStore.deleteRound`,
   `LiveScoringStore` rpc impl, `InMemoryScoringStore` no-op, `ScoringService.deleteRound`.
4. New `GamesRoom/Views/Components/SessionRoundTimeline.swift` — presentation-only
   view per spec §4 (rounds, members, onEdit, onDelete; pack icon, Round N,
   member + signed delta, corrected chip, trash button, empty state, a11y).
5. Five tests in `main.swift` per spec §5.

## Verification (mandatory, in order)

1. `./build-and-run-tests.sh` from repo root — all tests green, parse-check
   green. This is the project's test command; do not invent another.
2. `git diff --stat` — confirm no sibling files changed except the additive
   pbxproj registration.
3. Report: files changed, test count, parse-check result, any deviations.

## Out of scope

Dashboard surface, per-pack input forms, CAH count-based input, server-side
SQL execution (no local Postgres — migration is applied by the user later).
