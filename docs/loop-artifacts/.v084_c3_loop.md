# V0.84 C3 LOOP — No-Show Tax Host Prompt (execution log)

Spec: docs/loop-artifacts/V0.84_C3_SPEC.md (read it first — it is the contract).
Worktree root is the repo root. Do NOT touch GamesRoom/Services/MascotEngine.swift.

## Steps (execute in order, verify each)

1. Migration: write Supabase/migrations/082_no_show_tax_settings.sql per SPEC D2.
   Follow 081's file structure exactly (header comment, DO-block constraints,
   drop-then-create overload, grants, comments, notify pgrst).
2. Models: extend GamesRoom/Models/SeatDeposit.swift per SPEC D3 (enums,
   candidate struct, NoShowTaxPromptVoice).
3. Room model: four fields + keys + init + decode per SPEC D4.
4. Protocol + stores: RoomStoreProtocol.swift, RoomStore.swift (live RPC),
   InMemoryRoomStore.swift, RoomService.swift facade + cache mirrors per D4/D7/D7b.
   Every Room(…) reconstruction must carry the four new fields.
5. Settings UI: RoomSettingsSheet.swift four rows per D5 (Operations section,
   autosave wiring identical to autoCloseHours).
6. Prompt card: new GamesRoom/Views/NoShowTaxPromptCard.swift + pbxproj entries
   per D6. Render host-side in RoomDetailView.scrollBody (near HostWithdrawals
   section), gated isHost + live event + trigger==.prompt + candidates non-empty.
7. Tests: main.swift per D8 (≥ 6 cases).
8. Docs: bump Supabase/migrations/README.md high-water mark to 082.
9. Gates: ./build-and-run-tests.sh → green; python3 scripts/verify-xcode-project.py → green.
   Fix until green. Do NOT run the migration (the orchestrator applies it).
10. Commit everything as ONE commit: "feat(rooms): V0.84 C3 — no-show tax as
    host prompt + room settings". Do not push. Do not merge.

## Log (fill as you go)

- [step 1] done — `Supabase/migrations/082_no_show_tax_settings.sql` (4 columns + checks, 17-arg update_room_settings, get_my_rooms widened, list_no_show_candidates / apply_no_show_tax / skip_no_show_tax RPCs, grants, comments, notify pgrst).
- [step 2] done — `SeatDeposit.swift` gains `NoShowTaxTrigger` / `NoShowTaxDestination` enums, `NoShowTaxCandidate` (Identifiable + Codable + Hashable, server-shape keys), and `NoShowTaxPromptVoice` (mascot-attributed prompt / apply / skip copy; Swift string interpolation takes the mascot name from the caller per the directive).
- [step 3] done — `Room` gains `noShowTaxAmount` (default 200), `noShowTaxTrigger` (default `.prompt`), `noShowTaxGraceMinutes` (default 10), `noShowTaxDestination` (default `.nextPot`) with CodingKeys, init defaults, and decodeIfPresent fallbacks. Unknown raw values fall back to the locked defaults.
- [step 4] done — `RoomStoreProtocol` gains `loadNoShowCandidates` + `applyNoShowTax` + `skipNoShowTax`; `LiveRoomStore` wraps the three migration-082 RPCs (`NoShowTaxParams` struct); `InMemoryRoomStore` synthesises claimed-but-absent rows + mirrors apply/skip on the cache; `RoomService` exposes the three facade methods + `noShowCandidatesByEvent` cache + `cachedNoShowCandidates(eventId:)`. `updateRoom` signature widened to 17 params across all four files (protocol, live, in-memory, service); the two cache-mirror Room reconstructions (`setDrowningOptIn`, `setNotificationsEnabled`) carry the four new fields.
- [step 5] done — `RoomSettingsSheet` (hub) gains four `@State` bindings seeded from the room; `RoomSettingsOperationsSheet` accepts the four new bindings; the Operations sub-sheet renders a new "No-show tax" section with the four rows (`Stepper 0...1000 step 50`, `Picker` trigger, `Stepper 0...120` grace, `Picker` destination); autosave wires identical to `autoCloseHours` (four `onChange` bumpers + `writeAll` carries the four new params through `roomService.updateRoom`).
- [step 6] done — `GamesRoom/Views/NoShowTaxPromptCard.swift` (host-only card with mascot-attributed prompt line + Apply / Skip-texted / Skip-away buttons per candidate) + pbxproj entries (04B0 / 04B1 across PBXBuildFile, PBXFileReference, Views group, Sources phase). Card is rendered in `RoomDetailView.scrollBody` gated on `isHost + live event + trigger == .prompt + candidates non-empty`; `loadNoShowCandidatesIfNeeded` runs in the parallel `refresh`; `resolveNoShowTax` wraps Apply / Skip and routes errors through the existing `seatActionError` alert pattern. `Theme.Icon.handRaisedFill` added for the card header.
- [step 7] done — added 11 new cases in a `NoShowTaxTrigger / Destination / PromptVoice / Candidate decoding (V0.84 C3)` MARK section: trigger + destination decode round-trips, unknown-raw fallbacks (→ `.prompt` / `.nextPot`), `promptLine` carries mascot + display name + amount + `"apply?"`, `skipLine` per-reason variants, `NoShowTaxCandidate` server-shape decode + legacy fallback, `Room` decodes the four new columns (defaults + overrides), `InMemoryRoomStore.updateRoom` passthrough round-trip. Total count target: 196 + 11 = 207.
- [step 8] done — `Supabase/migrations/README.md` high-water mark bumped 071 → 082 (082_no_show_tax_settings.sql).
- [step 9] pending
- [step 10] pending
