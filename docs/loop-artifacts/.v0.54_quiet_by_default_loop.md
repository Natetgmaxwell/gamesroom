# V0.54 quiet-by-default loop (self-contained)

Read `V0.54_QUIET_BY_DEFAULT_SPEC.md` in this directory first — it is the
authoritative spec; this file is the loop contract.

Repo: `/Users/nathanmaxwell/Documents/VS Code/games-room` (workdir). Branch: main.
Do NOT create a branch. ONE commit at the end.

## Task

Implement quiet-by-default notifications per the V0.54 spec (derived from the
approved audit `docs/vision/QUIET_BY_DEFAULT_AUDIT.md`, commit 9de5040):
per-room notification opt-in default OFF, on-create push gated to opted-in +
non-declined, permission prompt off the join path, per-event one-tap mute, rooms-list
caption restricted to tonight/tomorrow, awards verified as season-close-only (no code
change).

## Hard constraints

1. **Exactly these files may change** (per spec D0 / "Files that may change"):
   `supabase/migrations/066_quiet_by_default_notifications.sql` (NEW),
   `GamesRoom/Models/Room.swift`, `GamesRoom/Models/Member.swift`,
   `GamesRoom/Models/EventRSVP.swift`, `GamesRoom/Services/RoomStoreProtocol.swift`,
   `GamesRoom/Services/RoomStore.swift`, `GamesRoom/Services/InMemoryRoomStore.swift`,
   `GamesRoom/Services/RoomService.swift`,
   `GamesRoom/Services/NotificationDispatcher.swift`,
   `GamesRoom/Views/JoinRoomSheet.swift`, `GamesRoom/Views/RoomPage.swift`,
   `GamesRoom/Views/RoomDetailView.swift`,
   `docs/loop-artifacts/V0.54_QUIET_BY_DEFAULT_SPEC.md` (deviations only),
   `docs/loop-artifacts/.v0.54_quiet_by_default_loop.md` (this file).
   Do NOT touch any other file. No new Swift files → no pbxproj change. There are
   untracked `.brainstorm/` and `docs/vision/RESEARCH_BRIEF_COUNTER_TREND.md` — never
   `git add -A`; commit only the listed files.
2. **Schema additions** (D1/D2): `room_memberships.notifications_enabled` (default
   false) + RPC `set_notifications_enabled(p_room_id, p_enabled)`; extend
   `get_my_rooms` and `get_room_members`; `event_rsvps.notifications_muted` (default
   false) + RPC `set_event_notifications_muted(p_event_id, p_muted)`; extend
   `get_event_rsvps`. Follow the `member_drowning_opt_in` pattern (migrations 044/045)
   exactly.
3. **D3 — no pinning model.** The on-create push reaches ONLY opted-in, non-declined,
   non-muted members. The one-tap mute is a general per-event escape hatch, not gated
   behind a nonexistent pinning concept. Record this deviation from audit item 4 in
   this loop file.

   **Recorded deviation:** Item 4 of the audit (`docs/vision/QUIET_BY_DEFAULT_AUDIT.md`,
   commit 9de5040) describes a "host-pinned" event type that "may reach opted-out
   members." There is no `pinned` boolean on `public.events` — every event is
   host-created. Implementing a host-pinning model now would require a new schema
   column + RLS extension + per-event fan-out override, all outside this loop's
   scope. Per D3 the hard item 2 wins: the on-create push reaches only opted-in,
   non-declined, non-muted members; the per-event mute is available to any opted-in
   member who received a push and wants to silence the next one. When a pinning
   concept lands in a future spec, it can override the opt-out gate with its own
   mute-style override.
4. **D4** — remove `requestNotificationsIfNeeded()` from `JoinRoomSheet` + its call +
   the `import UserNotifications`. Prompt surfaces via the dispatcher at night-of need.
5. **D9 UI** — `BriefingSlot` gains "Remind me about this room's nights" (opt-in
   toggle) + "Mute this event" (per-event mute). `RoomPage.socialProofCaption` gates
   to tonight/tomorrow (`isDateInToday` / `isDateInTomorrow`).
6. Match each file's existing conventions (Theme tokens, no dead code, no TODO, no
   comments that restate code). Keep the `.inPlay`/`.settleRound`/`.tonightEvent`
   hero structure intact.

## Deliverables
1. Migration 066 with both columns, both RPCs, and the three redefined read RPCs.
2. Swift: models + protocol + both store impls + RoomService + dispatcher gating.
3. UI: opt-in toggle + per-event mute in BriefingSlot; join-path prompt removed;
   caption gated.
4. Awards: verified season-close-only (no code change).

   **Awards verification (D10 — no code change, verified by grep):**
   - `AwardsCard` is rendered ONLY by the `.seasonClose` V0State branch in
     `RoomDetailView.activeSlot` (the `case .seasonClose(let season, let awards):`
     arm). No other call site invokes it.
   - The `.seasonClose` branch resolves only when `currentSeason.status == .ended`
     (the `RoomDetailView.state` resolver's M1.1 case).
   - No badge-count API, no "3 new awards" nudge, no standing awards card on the
     quiet-room screen are present in `Views/` or `Services/`.
   - The `DrowningBadge` opt-in toggle is recipient-only and persists via the
     `set_drowning_opt_in` RPC (migration 045). The other awards
     (phoenix / veteran / whale) are read-only by design.
   - Conclusion: awards remain private-to-recipient for drowning; the only public-
     surface awards card is gated to season-close. No code change required.

## Verification (mandatory, in order)
1. `./build-and-run-tests.sh` — all green, count ≥160, parse-check ≥44.
2. `python3 scripts/verify-xcode-project.py` — green.
3. `git diff --stat` — only the listed files.
4. `git diff` review — confirm all six gating points + both schema additions.
5. Commit once with a clear message (e.g. "feat: quiet-by-default notifications —
   per-room opt-in, event mute, prompt timing (V0.54)").
