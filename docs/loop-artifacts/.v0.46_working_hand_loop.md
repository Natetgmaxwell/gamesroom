# V0.46 working-hand loop (self-contained)

Read `V0.46_WORKING_HAND_SPEC.md` in this directory first — it is the
authoritative spec; this file is the loop contract.

Repo: `/Users/nathanmaxwell/Documents/VS Code/games-room` (workdir). Branch:
main. Do NOT create a branch.

## Task

After a casino member withdraws chips, surface their working hand in the
in-play witness hero, and give casino members BOTH settle paths (vision
chip-scan AND a manual SettleCasinoSheet) instead of hard-routing to scan
only. CAH keeps its card-count scan; the host settle flow is unchanged.

## Hard constraints

1. **Exactly these files may change:**
   - `GamesRoom/Views/RoomDetailView.swift`
   - `GamesRoom/Theme.swift` (one additive `Icon` constant only)
   - `docs/loop-artifacts/V0.46_WORKING_HAND_SPEC.md` (only if a deviation
     must be recorded)
   - `docs/loop-artifacts/.v0.46_working_hand_loop.md` (this file)
   Do NOT touch: pbxproj, any migration, WithdrawChipsSheet.swift,
   ChipScanSheet.swift, SettleCasinoSheet.swift, CAHCardScanSheet.swift,
   main.swift, any other file. There are untracked `.designs/` and `build/`
   dirs — never `git add -A`, never commit anything outside this list.
2. **No new Swift files.** No pbxproj change. No migration.
3. **WitnessSlot** is a private struct inside RoomDetailView.swift — edit it
   in place (do not extract to a Components/ file).
4. **Member manual settle:** add a NEW `@State private var
   manualSettleSheetEvent: Event?` and a `.sheet(item:)` binding that ALWAYS
   presents `SettleCasinoSheet` (withdrawn: casinoWithdrawn, inject
   scoringService + authService). Do NOT reuse the host's `settleSheetEvent`
   for the member manual link.
5. **Working hand badge** renders only when `workingHand > 0`. Pass
   `casinoWithdrawn` for casino `.inPlay`/`.settleRound`; pass nil for CAH and
   for `.tonightEvent` (no withdrawal yet).
6. **Manual link** renders only for non-host casino members, under the scan
   CTA (one tap behind), via the new `manualSettleAction(for:)` helper. Hosts
   and CAH members get nil. The host "Score a round" / scan path must remain
   exactly as-is.
7. Match each file's existing conventions (Theme.Typography, Theme.Palette,
   `.pressScale()`, `Theme.Icon`). No dead code, no TODOs, no comments that
   restate the code.

## Deliverables

1. Working-hand badge on the casino `.inPlay` witness hero showing the
   withdrawn amount (matches `casinoWithdrawn`).
2. Casino member: scan CTA → ChipScanSheet (unchanged); "Settle manually" link
   one tap behind → SettleCasinoSheet.
3. CAH: no badge, no manual link; card-count scan unchanged.
4. Host: scan and "Score a round" → SettleCasinoSheet unchanged; no manual
   link.

## Verification (mandatory, in order)

1. `./build-and-run-tests.sh` from repo root — all green. Report the test
   count.
2. `./scripts/parse-check-swiftui.sh` — green.
3. `./scripts/verify-xcode-project.py` — green (pbxproj untouched).
4. `git diff --stat` — exactly the files above.
5. Do NOT commit. Report: files changed, test count, parse-check result,
   verify-xcode-project result, any deviation.
