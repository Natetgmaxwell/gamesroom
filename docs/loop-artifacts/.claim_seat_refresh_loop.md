# Claim-seat refresh loop (self-contained)

Read `CLAIM_SEAT_REFRESH_SPEC.md` in this directory first — it is the
authoritative spec; this file is the loop contract.

Repo: `/Users/nathanmaxwell/Documents/VS Code/games-room` (workdir). Branch:
main. Do NOT create a branch. Do NOT commit.

## Path discipline (critical — a prior run was auto-rejected)

Your process workdir IS the repo root. Use ONLY relative paths in every
shell command. NEVER type `/Users/nathanmaxwell/Documents/VS Code/...` or any
absolute path containing a space — the sandbox auto-rejects those as
external directories and the run dies. `ls`, `git status`, `git diff`,
`./build-and-run-tests.sh`, `./scripts/parse-check-swiftui.sh` all run from
the root with relative paths. File reads via your editor/grep tools use
repo-relative paths (they worked fine).

## Task

Fix "claim seat doesn't refresh": after a successful RSVP upsert the seat
grid, briefing seat count, and slot state must update. Surface claim/decline/
release errors visibly instead of swallowing them.

## Hard constraints

1. **Exactly five files may change:**
   - `GamesRoom/Services/RoomService.swift`
   - `GamesRoom/Views/RoomDetailView.swift`
   - `main.swift`
   - `docs/loop-artifacts/CLAIM_SEAT_REFRESH_SPEC.md` (only if a deviation
     must be recorded)
   - `docs/loop-artifacts/.claim_seat_refresh_loop.md` (this file)
   Do NOT touch: Theme.swift, pbxproj, any migration, any other Swift file.
   Never `git add -A`; never commit anything outside this list.
2. **No new Swift files.** No pbxproj change. No migration, no RPC change.
3. **TDD where possible.** The Foundation runner covers Models + Packs +
   InMemoryRoomStore. Add a store-contract test in main.swift that proves
   `upsertEventRSVP` followed by `fetchEventRSVPs` reflects the new state
   (the seat-grid data path). Do not add tests for SwiftUI view logic (the
   runner can't cover it).
4. **No dead code, no TODOs, no comments that restate the code.** Match each
   file's existing header/MARK/doc-comment conventions.
5. **Backward-compatible service.** `upsertEventRSVP` keeps its signature and
   return type. The reloads are additive inside the method.
6. **Error surfacing is exact.** In RoomDetailView, `claimSeat` /
   `declineSeat` / `releaseSeat` must not swallow the error. Add a view-local
   `@State` error message + a `.alert` on the body that presents it. The
   alert must be reachable from all three seat actions. Do not change the
   existing `lastError` handling in the service.

## Deliverables

1. `RoomService.upsertEventRSVP`: after the store write + `rsvpByEvent`
   cache update, call `_ = await loadEventRSVPs(eventId:)` and
   `_ = await loadBriefing(eventId:)` so the seat grid and briefing seat
   count refresh. Keep the existing P1.3 notification re-schedule.
2. RoomDetailView: `claimSeat` / `declineSeat` / `releaseSeat` set a
   view-local error state on failure; the body presents it via `.alert`.
3. main.swift: a store-contract test proving the seat-grid data path
   reflects a claim (upsert then fetchEventRSVPs shows the claimed state).

## Verification (mandatory, in order)

1. `./build-and-run-tests.sh` from repo root — all green. Report the new
   test count (136 will shift).
2. `./scripts/parse-check-swiftui.sh` — green.
3. `git diff --stat` — exactly the five files above.
4. Do NOT commit.
5. Report: files changed, test count, parse-check result, deviations.

## Baseline

At loop start the tree was NOT clean: `GamesRoom/Views/RoomDetailView.swift`
has uncommitted sibling WIP (pack-config save fix, lines ~1957-2140) and
untracked loop files exist (`.seat_grid_maxseats_loop.md`,
`SEAT_GRID_MAXSEATS_SPEC.md`, `.v0_39_packconfig_save_loop.md`). Do NOT
touch, revert, or merge that sibling work. Your edits to RoomDetailView.swift
are confined to `claimSeat`/`declineSeat`/`releaseSeat` (~758-789) + a
body-level `.alert`; do not disturb the pack-config WIP. 136/136 Foundation
tests passed and parse-check 42/42 at loop start.

## Run results (this loop)

- `RoomService.upsertEventRSVP` reloads `eventRSVPsByEvent` and
  `briefingByEvent` after the store write + `rsvpByEvent` cache
  update; P1.3 notification re-schedule retained.
- `RoomDetailView` adds `seatActionError` + `showSeatActionError`
  `@State` and a body-level `.alert` reachable from
  `claimSeat` / `declineSeat` / `releaseSeat`. Errors no longer
  swallowed.
- `main.swift` adds two `InMemoryRoomStore` store-contract tests
  exercising the `upsertEventRSVP` return-value round-trip and
  the briefing seat-count mirror.
- Deviations: full `upsertEventRSVP` → `fetchEventRSVPs`
  seat-grid data-path test is blocked by a pre-existing
  `InMemoryRoomStore.fetchEventRSVPs` indexing bug (events keyed
  by roomId, read keys by eventId). Out of this loop's 5-file
  change contract — recorded in `CLAIM_SEAT_REFRESH_SPEC.md`
  §Deviations. Briefing path test covers the seam the bug report
  names ("seat count in the briefing updates").
- `./build-and-run-tests.sh`: 146/146. Parse-check 42/42.
- No branch, no commit.
