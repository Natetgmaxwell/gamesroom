# V0.35 season-history-v2 loop (self-contained)

Read `V0.35_SEASON_HISTORY_V2_SPEC.md` in this directory first — it is the
authoritative spec; this file is the loop contract.

Repo: `/Users/nathanmaxwell/Documents/VS Code/games-room` (workdir). Branch:
main. Do NOT create a branch.

## Task

Redesign the season-history card per user feedback: drop the "Season N"
label, minimal three-element rows (date/subtitle → total → delta), and add an
intra-season trend sparkline fed by a new `score_progression` column on the
`get_season_history` RPC.

## Hard constraints

1. **Exactly six files may change:**
   - `Supabase/migrations/057_season_history_progression.sql` (NEW)
   - `GamesRoom/Models/SeasonHistoryEntry.swift`
   - `GamesRoom/Views/RoomDetailView.swift`
   - `GamesRoom/Services/InMemoryRoomStore.swift`
   - `main.swift`
   - `docs/loop-artifacts/V0.35_SEASON_HISTORY_V2_SPEC.md` (only if a
     deviation must be recorded)
   Do NOT touch: Theme.swift, pbxproj, any other migration, any other Swift
   file. The working tree has sibling WIP — never `git add -A`, never commit
   anything outside this list.
2. **No new Swift files.** `SeasonScorePoint` lives inside
   SeasonHistoryEntry.swift. No pbxproj change.
3. **TDD where possible.** The Foundation runner covers Models + Packs.
   Update main.swift tests per the spec: remove the two `displayName` tests,
   add progression decode + store assertions. Do not add tests for SwiftUI
   view logic (the runner can't cover it).
4. **Theme tokens only.** Theme.Palette, Theme.Typography, Theme.Layout,
   Theme.Icon. No new tokens, no hardcoded colors/fonts/sizes.
5. **No dead code, no TODOs, no comments that restate the code.** Match each
   file's existing header/MARK/doc-comment conventions. Removing the rank
   pill means removing `rankPill`, `rankText`, `ordinal`, and `displayName`
   — no dead code left behind.
6. **No Swift Charts import.** The sparkline is a hand-rolled `Path`
   polyline. The codebase has zero Charts precedent and the parse-check
   environment must keep working.
7. **Backward-compatible decode.** `scoreProgression` decodes with
   `decodeIfPresent ?? []` so the in-memory store and pre-057 servers don't
   break.

## Deliverables

1. Migration 057 exactly as specified (create or replace
   `get_season_history` with the `score_progression jsonb` column, the
   lateral progression CTE, membership guard preserved, grant + comment
   updated).
2. Model: `SeasonScorePoint` nested type, `scoreProgression` field,
   `displayName` removed, doc comment updated to reference 057.
3. View: `SeasonHistorySection` redesigned — subtitle-or-date primary text,
   total + delta cluster (no "pts", no "vs now", no rank pill), full-width
   `TrendSparkline` when `scoreProgression.count >= 2`, accessibility label
   updated. Dead helpers removed.
4. InMemoryRoomStore: both Carwoola seasons seeded with ascending
   progression points inside their date windows, last point == callerTotal.
5. main.swift: displayName tests removed; store test asserts progression
   non-empty + last point == callerTotal; new decode test for
   `score_progression` present and absent.

## Verification (mandatory, in order)

1. `./build-and-run-tests.sh` from repo root — all green, parse-check green.
   Report the new test count (87 will shift).
2. `git diff --stat` — exactly the six files above.
3. Commit (one logical change):
   `feat: season history card v2 — minimal layout + intra-season trend (V0.35)`
4. Report: files changed, test count, parse-check result, deviations.
