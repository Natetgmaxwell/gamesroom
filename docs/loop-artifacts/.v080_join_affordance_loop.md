V0.80 join affordance — two edits, no new files, exact spec compliance.

CONTEXT
- Spec: docs/loop-artifacts/V0.80_JOIN_AFFORDANCE_SPEC.md (read it).
- RoomPage.swift already has `showingJoinRoom` @State, `openJoinRoom()`, and a `.sheet(isPresented: $showingJoinRoom)` presenting JoinRoomSheet. Do NOT touch those.
- Theme.swift `enum Icon` (line ~126-188) is the icon token registry. No door/enter icon exists.

EDIT 1 — GamesRoom/Theme.swift, inside `enum Icon`, after the `bellSlashFill` token (line ~187):
Add ONE new token with a doc comment following the V0.79 bell pattern:
    /// V0.80 — the join-room toolbar affordance for existing members.
    static let personCropCircleBadgePlus = "person.crop.circle.badge.plus"

EDIT 2 — GamesRoom/Views/RoomPage.swift, in `toolbarContent` (line ~505-533):
Add a second trailing toolbar item INSIDE the existing `if !roomService.rooms.isEmpty` block, AFTER the create (+) ToolbarItem, so it reads + then join (door). Mirror the plus button's structure exactly:
    ToolbarItem(placement: .topBarTrailing) {
        Button(action: openJoinRoom) {
            Image(systemName: Theme.Icon.personCropCircleBadgePlus)
                .foregroundStyle(Theme.Palette.primaryText)
        }
        .accessibilityLabel(Text("Join a room with a code"))
        .accessibilityHint(Text("Enter a friend's join code to become a member"))
    }

CONSTRAINTS
- No new files. No pbxproj changes. No server work. No JoinRoomSheet changes. No copy changes to the empty state.
- SF Symbol person.crop.circle.badge.plus is iOS 15+; deployment target 26.0 — safe.
- Do not reorder existing toolbar items (gearshape stays last, host-gated).
- Do not touch any other code. Match surrounding style exactly.

DONE WHEN
- `git diff` shows exactly 2 files changed with only the additions above.
