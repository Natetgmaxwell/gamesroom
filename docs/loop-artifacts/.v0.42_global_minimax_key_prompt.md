You are implementing V0.42 for the Games Room iOS app.

Read these two files first — they are your complete contract:
1. docs/loop-artifacts/V0.42_GLOBAL_MINIMAX_KEY_SPEC.md (the spec)
2. docs/loop-artifacts/.v0.42_global_minimax_key_loop.md (the loop contract — hard constraints, deliverables, verification)

Follow the loop contract exactly. The spec describes the changes; the loop
file lists the exact files you may touch and the mandatory verification
steps. Do NOT touch Config.xcconfig (already updated, gitignored). Do NOT
print or commit the MiniMax key value. Do NOT commit anything.

When done, run the three verification commands in order and report: files
changed, test count, parse-check result, verify-xcode-project result, any
deviation from the spec.
