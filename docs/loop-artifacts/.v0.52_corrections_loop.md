# V0.52 corrections loop (self-contained)

Read `V0.52_CORRECTIONS_SPEC.md` in this directory first — it is the
authoritative spec; this file is the loop contract.

Repo: `/Users/nathanmaxwell/Documents/VS Code/games-room` (workdir). Branch: main.
Do NOT create a branch. Do NOT commit.

## Task

Three user corrections on V0.51's chips-on-the-table + settle flow. All
UI-layer. Per SPEC Change 1: rename section to "Chips withdrawn", drop the
host-only bank column, add caption "Chips moved from the bank for tonight".
Per Change 2: pack-name the WitnessSlot settle CTAs ("Settle casino chips" /
"Count your CAH cards") at all three call sites + the default. Per Change 3:
add pack-explicit context headers to ChipScanSheet and CAHCardScanSheet.
Per Change 4: verify (read-only) the withdraw guardrail.

## Hard constraints

1. **Exactly these files may change:**
   - `GamesRoom/Views/RoomDetailView.swift`
   - `GamesRoom/Views/ChipScanSheet.swift`
   - `GamesRoom/Views/CAHCardScanSheet.swift`
   - `docs/loop-artifacts/V0.52_CORRECTIONS_SPEC.md` (only to record deviations)
   - `docs/loop-artifacts/.v0.52_corrections_loop.md` (this file)
   Do NOT touch any other file. No new Swift files → no pbxproj change.
   There are untracked `.designs/` and `build/` dirs — never `git add -A`,
   never commit anything outside this list.
2. **No schema change.** Do NOT touch migrations, `WorkingHand.swift`, or
   `CasinoService.loadWorkingHands`. `WorkingHand.pointsBalance` stays in
   the model; only the RENDER drops.
3. Match each file's existing conventions (Theme.Typography, Theme.Palette,
   `.font(Theme.Typography.caption)`, `.foregroundStyle(Theme.Palette.primaryText.opacity(0.55))`).
   No dead code, no TODOs, no comments that restate the code.
4. Do NOT alter the `.inPlay`/`.settleRound`/`.tonightEvent` hero structure,
   `HostWithdrawalsSection`, the standings, or the mascot footer.

## Deliverables

1. `ChipsOnTableSection` header = "Chips withdrawn"; muted caption "Chips
   moved from the bank for tonight"; no bank column (host or member).
2. WitnessSlot `scanTitle` at `.inPlay`, `.tonightEvent`, `.settleRound`
   = `isCAH ? "Count your CAH cards" : "Settle casino chips"`; default =
   "Settle casino chips".
3. ChipScanSheet cameraView intro gains a header line naming the Casino
   hand + `withdrawn` pts (uses existing `withdrawn` param).
4. CAHCardScanSheet cameraView intro gains a header line naming CAH cards.
5. Guardrail verification recorded (no code change).

## Verification (mandatory, in order)

1. `./build-and-run-tests.sh` from repo root — all green. Report the test
   count (expect 160) and parse-check file count (expect 44).
2. `python3 scripts/verify-xcode-project.py` — green (unchanged, but run it).
3. `git diff --stat` — exactly the 3 Swift files + 2 loop/spec docs.
4. `git diff` review — confirm the bank column is GONE, captions present,
   CTA labels pack-named at all 3 sites, sheet headers present.
5. Do NOT commit. Report: files changed, test count, parse-check result,
   verify-xcode-project result, guardrail findings, any deviation.
