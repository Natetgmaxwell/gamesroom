# V0.37 seat-roster social proof loop (self-contained)

Read `V0.37_SEAT_ROSTER_SOCIAL_PROOF_SPEC.md` in this directory first — it is
the authoritative spec; this file is the loop contract.

Repo: `/Users/nathanmaxwell/Documents/VS Code/games-room` (workdir). Branch:
main. Do NOT create a branch. Do NOT commit.

## Path discipline (critical — a prior run was auto-rejected)

Your process workdir IS the repo root. Use ONLY relative paths in every
shell command. NEVER type `/Users/nathanmaxwell/Documents/VS Code/...` or any
absolute path containing a space — the sandbox auto-rejects those as
external directories and the run dies. `ls`, `git status`, `git diff`,
`./build-and-run-tests.sh`, `./scripts/parse-check-swiftui.sh` all run from
the root with relative paths. File reads via your editor/grep tools use
repo-relative paths (they worked fine).

## Task

Surface claimed-seat member names for social proof: a one-line caption on the
rooms LIST rows (replacing the static "Tap to open" subtitle when an event has
claimants) and on the room detail's briefing card below the seat grid.
Pre-play events only (`startedAt == nil`).

## Hard constraints

1. **Exactly eight files may change:**
   - `GamesRoom/Models/EventRSVP.swift`
   - `GamesRoom/Services/RoomService.swift`
   - `GamesRoom/Views/RoomPage.swift`
   - `GamesRoom/Views/RoomDetailView.swift`
   - `main.swift`
   - `docs/loop-artifacts/V0.37_SEAT_ROSTER_SOCIAL_PROOF_SPEC.md` (only if a
     deviation must be recorded)
   - `docs/loop-artifacts/.v0.37_seat_roster_social_proof_loop.md` (this file)
   - `docs/loop-artifacts/.v0.37_seat_roster_social_proof_prompt.md`
   Do NOT touch: Theme.swift, pbxproj, any migration, any other Swift file.
   Never `git add -A`; never commit anything outside this list.
2. **No new Swift files.** No pbxproj change. No migration, no RPC change.
3. **TDD where possible.** The Foundation runner covers Models + Packs.
   `SocialProof.claimedSeatsCaption` lives in `GamesRoom/Models/EventRSVP.swift`
   (pure Foundation — no SwiftUI import). Write tests in main.swift per the
   spec before implementing the formatter. Do not add tests for SwiftUI view
   logic (the runner can't cover it).
4. **Theme tokens only.** No new tokens, no hardcoded colors/fonts/sizes.
5. **No dead code, no TODOs, no comments that restate the code.** Match each
   file's existing header/MARK/doc-comment conventions.
6. **Backward-compatible service.** `loadRoomsSocialProof()` is additive;
   no existing method signature changes. `RoomPage`'s `.task`/`.refreshable`
   append the call after `refresh()` — do not reorder or remove existing
   loads.
7. **Caption formatting is exact** per spec decision 1: 0 → nil (silent),
   1 → "Sarah has claimed a seat", 2 → "Sarah and Mike have claimed seats",
   3+ → "Sarah, Mike +N more have claimed seats". Names trimmed, empties
   dropped before counting.
8. **Row subtitle swap.** The caption REPLACES "Tap to open" when present;
   "Tap to open" remains otherwise. One subtitle line, never two. Both iPhone
   list rows and iPad sidebar rows get the same treatment. Hero card: no
   social proof (spec decision 5).

## Deliverables

1. `SocialProof` enum in EventRSVP.swift with
   `claimedSeatsCaption(claimedNames:)` per spec decision 1.
2. `RoomService.loadRoomsSocialProof()` per spec decision 3 (cache-hit
   short-circuits; pre-play gate `startedAt == nil`).
3. RoomPage: `.task` + `.refreshable` call it; iPhone row and iPad sidebar
   row subtitles per spec decision 4.
4. RoomDetailView `BriefingSlot`: caption below the seat grid when rsvps are
   loaded (spec decision 6).
5. main.swift: formatter tests per spec file-by-file item 5.

## Verification (mandatory, in order)

1. `./build-and-run-tests.sh` from repo root — all green. Report the new
   test count (111 will shift).
2. `./scripts/parse-check-swiftui.sh` — green.
3. `git diff --stat` — exactly the eight files above.
4. Do NOT commit.
5. Report: files changed, test count, parse-check result, deviations.

## Baseline

At loop start the tree was clean at `209a24f`; 111/111 Foundation tests
passed; parse-check 39/39. If the tree is no longer clean, stop and report —
do not merge or fix sibling work.
