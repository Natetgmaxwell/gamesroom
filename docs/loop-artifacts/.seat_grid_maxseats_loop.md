# Seat grid maxSeats loop (self-contained)

Read `SEAT_GRID_MAXSEATS_SPEC.md` in this directory first — it is the
authoritative spec; this file is the loop contract.

Repo: `/Users/nathanmaxwell/Documents/VS Code/games-room` (workdir). Branch:
main. Do NOT create a branch. Do NOT commit.

## Path discipline (critical — a prior run was auto-rejected)

Your process workdir IS the repo root. Use ONLY relative paths in every
shell command. NEVER type `/Users/nathanmaxwell/Documents/VS Code/...` or any
absolute path containing a space — the sandbox auto-rejects those as
external directories and the run dies. `ls`, `git status`, `git diff`,
`./build-and-run-tests.sh`, `./scripts/parse-check-swiftui.sh` all run from
the root with relative paths. File reads via your editor/grep tools use
repo-relative paths (they worked fine).

## Task

Fix the seat grid so it renders `maxSeats` cells (claimed seats first, the
rest open chairs labelled "open") instead of one cell per member. The grid
must show 6 cells for a 6-seat room even when only 2 members have claimed.

## Hard constraints

1. **Exactly six files may change:**
   - `GamesRoom/Models/EventRSVP.swift`
   - `GamesRoom/Views/RoomDetailView.swift`
   - `main.swift`
   - `docs/loop-artifacts/SEAT_GRID_MAXSEATS_SPEC.md` (only if a deviation
     must be recorded)
   - `docs/loop-artifacts/.seat_grid_maxseats_loop.md` (this file)
   - `docs/loop-artifacts/.seat_grid_maxseats_prompt.md`
   Do NOT touch: Theme.swift, pbxproj, any migration, any other Swift file.
   Never `git add -A`; never commit anything outside this list.
2. **No new Swift files.** No pbxproj change. No migration, no RPC change.
3. **TDD where possible.** The Foundation runner covers Models + Packs.
   `SeatGrid` lives in `GamesRoom/Models/EventRSVP.swift` (pure Foundation —
   no SwiftUI import). Write tests in main.swift per the spec before
   implementing the derivation. Do not add tests for SwiftUI view logic (the
   runner can't cover it).
4. **Theme tokens only.** No new tokens, no hardcoded colors/fonts/sizes.
5. **No dead code, no TODOs, no comments that restate the code.** Match each
   file's existing header/MARK/doc-comment conventions.
6. **Backward-compatible.** `SeatGridRow` gains `maxSeats: Int`; the existing
   per-cell rendering for claimed seats is preserved. `BriefingSlot` passes
   `event.maxSeats` and renders the grid when `maxSeats > 0` (drop the
   `!rsvps.isEmpty` gate). The social-proof caption below the grid still
   gates on claimed names — do not change `SocialProof`.
7. **Cell derivation is exact** per spec decision 2: exactly `maxSeats` cells,
   claimed RSVPs in input order fill the first N, the rest are open (nil
   rsvp). Declined/unclaimed members are open chairs, not cells. Clamp
   `maxSeats` to >= 0 (0 → empty). Claimed > maxSeats → extra claimed dropped.
8. **Column count** per spec decision 3: `max(2, Int(ceil(sqrt(Double(maxSeats)))))`.
   6 → 3, 4 → 2, 8 → 3, 12 → 4, never fewer than 2.

## Deliverables

1. `SeatGrid` enum in EventRSVP.swift with `Cell`, `cells(maxSeats:rsvps:)`,
   `columnCount(for:)` per spec decisions 1-3.
2. RoomDetailView `SeatGridRow`: `maxSeats` property, `columns` via
   `SeatGrid.columnCount(for:)`, body iterates `SeatGrid.cells(maxSeats:rsvps:)`,
   `seatCell` handles a `Cell` (nil rsvp → open chair + "open" label + open
   accessibility label). `BriefingSlot` passes `maxSeats: event.maxSeats` and
   renders the grid when `event.maxSeats > 0`.
3. main.swift: `SeatGrid` tests per spec file-by-file item 3.

## Verification (mandatory, in order)

1. `./build-and-run-tests.sh` from repo root — all green. Report the new
   test count (111 will shift).
2. `./scripts/parse-check-swiftui.sh` — green.
3. `git diff --stat` — exactly the six files above.
4. Do NOT commit.
5. Report: files changed, test count, parse-check result, deviations.

## Baseline

At loop start the tree was clean at `e031ee4`; 111/111 Foundation tests
passed. If the tree is no longer clean, stop and report — do not merge or fix
sibling work.
