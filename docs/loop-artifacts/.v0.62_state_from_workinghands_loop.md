# V0.62 state-from-workinghands loop (self-contained)

Read `V0.62_STATE_FROM_WORKINGHANDS_SPEC.md` in this directory — it is the
authoritative spec; this file is the loop contract.

Repo: `/Users/nathanmaxwell/Documents/VS Code/games-room` (workdir).
Do NOT create a branch. Do NOT commit.

## Task

Bug: Test Room shows "Withdraw to play" (state `.tonightEvent`) even after the
host withdrew 100 pts, because the state machine keys off `casinoWithdrawn`,
which comes only from the `get_my_open_withdrawal` RPC that returns empty on
the live DB. The V0.51 `get_event_working_hands` RPC (loaded into
`workingHands`) WORKS. Fix: drive the state machine and the settle surfaces
from `workingHands`.

## Current code facts (verified — do not re-verify)

- `RoomDetailView.swift`:
  - `@State private var casinoWithdrawn: Int = 0` (line 73)
  - `@State private var workingHands: [WorkingHand] = []` (line 81)
  - `private var currentUserId: UUID?` (line 152)
  - State branch (lines ~514-522): `if isLive && casinoWithdrawn == 0 { return .tonightEvent }` then `if isLive { return .inPlay }`
  - `loadMyOpenWithdrawalIfNeeded()` (lines ~947-954) sets casinoWithdrawn from RPC
  - `loadWorkingHandsIfNeeded()` (lines ~984-992) sets `workingHands = await casinoService.loadWorkingHands(eventId:)`
  - `refresh()` line ~824: `async let withdrawalLoad: () = loadMyOpenWithdrawalIfNeeded()`; await at line ~833
  - `casinoWithdrawn` also feeds: ChipScanSheet(:319), SettleCasinoSheet member(:349), SettleCasinoSheet host(:366), MascotFooterCaption(:475), WitnessSlot workingHand badge(:714,:777)
- `WorkingHand` (Models/WorkingHand.swift): `memberId: UUID`, `workingHand: Int`, `pointsBalance: Int`, `id = memberId`.
- `CasinoService.loadMyOpenWithdrawal(eventId:)` non-throwing, returns `CasinoWithdrawal?`. `CasinoWithdrawal.pointsWithdrawn: Int`.

## Changes (apply exactly)

1. Add computed property near `currentUserId`:
   `private var myWorkingHand: Int { workingHands.first(where: { $0.memberId == currentUserId })?.workingHand ?? 0 }`
2. State branch: change `isLive && casinoWithdrawn == 0` -> `isLive && myWorkingHand == 0`. Keep `.settleRound` precedence and `.inPlay` for `isLive`.
3. In `loadWorkingHandsIfNeeded()`, after `workingHands = await ...`, add `casinoWithdrawn = myWorkingHand`. Keep `casinoWithdrawn` @State — it is now fed from workingHands, so all existing call sites (sheets, mascot, badge) stay correct without changes.
4. Delete `loadMyOpenWithdrawalIfNeeded()` entirely. Remove its `async let withdrawalLoad` line from `refresh()` and remove `withdrawalLoad` from the `await` tuple. The settle sheets never need the row id, only the points amount.
5. Do NOT touch MascotEngine, models, or the RPC. Leave `CasinoService.loadMyOpenWithdrawal` and migration 040 in place (documented no-longer-authoritative).

## Verification (mandatory, in order)

1. `./build-and-run-tests.sh` from repo root — all green. Report test count and parse-check file count.
2. `python3 scripts/verify-xcode-project.py` — green.
3. `git diff --stat` — only the three allowed files.
4. Do NOT commit. Report: files changed, test count, parse-check result, verify-xcode-project result, deviations from SPEC.
