# V0.48 mascot-state-aware loop (self-contained)

Read `V0.48_MASCOT_STATE_AWARE_SPEC.md` in this directory first — it is the
authoritative spec; this file is the loop contract.

Repo: `/Users/nathanmaxwell/Documents/VS Code/games-room` (workdir). Branch:
main. Do NOT create a branch.

## Task

Make the room-page mascot footer narrate the live circumstance, not just the
broad room state. Today `MascotEngine.footerKind` collapses the 10-case
`V0State` machine to 4 footer kinds. Add 4 new `NotificationKind` footer
cases — `tonightEvent`, `inPlayWithWithdrawal`, `settleRound`, `seasonClose` —
with 25 new matrix cells each (transcribed verbatim from spec §8), extend
`footerKind` to resolve them from `(activeEvent, leaderboard, withdrawnAmount,
currentSeason)`, extend `RoomContext` with `withdrawnAmount` / `lastWinnerDelta`
/ `seasonDaysLeft`, add `{working_hand}` / `{last_delta}` / `{season_days_left}`
to the nil-preserving sentence-drop set, add a `lastWinnerDelta` derivation,
and wire the view to pass `withdrawnAmount` + `currentSeason`.

`.justSettled` reuses the existing `.postPlayRecap` kind — do not add a
justSettled case. `{last_winner}` reuses `recentWinnerNames.first` — do not add
a last-winner context field.

## Hard constraints

1. **Exactly six files may change:**
   - `GamesRoom/Services/MascotEngine.swift`
   - `GamesRoom/Views/RoomDetailView.swift`
   - `main.swift`
   - `docs/loop-artifacts/V0.48_MASCOT_STATE_AWARE_SPEC.md`
   - `docs/loop-artifacts/.v0.48_mascot_state_aware_loop.md` (this file)
   - `docs/loop-artifacts/.v0.48_mascot_state_aware_prompt.md` (this dispatch's
     driver prompt)
   Do NOT touch: NotificationDispatcher.swift, Theme.swift, pbxproj, any
   migration, any other Swift file. The working tree has untracked
   `.designs/app-icons-v4/` — never `git add -A`, never commit anything outside
   this list.
2. **No new Swift files.** No pbxproj change. No migration (pure client-side).
3. **Transcribe the 100 matrix cells VERBATIM from spec §8.** The voice design
   is pinned; do not re-word cells. If a cell cannot be transcribed exactly,
   stop and record the deviation in the spec's Decisions section instead of
   improvising.
4. **Placeholder contract:** add exactly `{working_hand}`, `{last_delta}`,
   `{season_days_left}` to the nil-preserving + sentence-drop set alongside the
   existing {event}, {winner}, {leader}, {caller_rank}, {event_count},
   {days_quiet}, {time}, {venue}, {seats_left}, {seats_claimed}. {mascot},
   {room}, {member_count} stay always-substituted. {date} and {host_note} keep
   `""` substitution. Do NOT add `{ordinal}`.
5. **`footerKind`** gains defaulted `withdrawnAmount: Int = 0` and
   `currentSeason: Season? = nil` params so the existing 6 test call sites keep
   compiling. Resolution order exactly as spec §2.
6. **`RoomContext`** gains `withdrawnAmount: Int? = nil`, `lastWinnerDelta:
   Int? = nil`, `seasonDaysLeft: Int? = nil` in BOTH the memberwise and the
   explicit init, so `NotificationDispatcher`'s 4-arg construction compiles.
7. **`buildLLMPrompt`** gains the 4 new "Message type:" lines and the
   working-hand / last-winner-delta / season-days-left context lines per spec
   §6.
8. Match each file's existing header/MARK/doc-comment conventions. No dead
   code, no TODOs, no comments that restate the code. The engine header
   placeholder-comment block must list the three new placeholders.

## Implementation order

1. `MascotEngine.swift`: add 4 `NotificationKind` cases → extend
   `RoomContext` + explicit init → add `lastWinnerDelta` → extend `footerKind`
   → extend `interpolate` nil-preserving set → extend `buildLLMPrompt` switch +
   context → add the 100 matrix cells to `templateFor` (verbatim §8).
2. `RoomDetailView.swift`: add `withdrawnAmount` + `currentSeason` props to
   `MascotFooterCaption`, pass them into `footerKind` and `RoomContext`; update
   the call site at line ~441 to pass them.
3. `main.swift`: update the 6 existing footerKind tests if needed (defaulted
   params — should compile unchanged) and add the new tests from spec's Test
   plan. `makeEvent` may need a `hostFinalized` variant.

## Verification (mandatory, in order)

1. `./build-and-run-tests.sh` from repo root — all green. Report the new test
   count.
2. `python3 scripts/verify-xcode-project.py` — green.
3. `git diff --stat` — exactly the six files above.
4. Do NOT commit — the orchestrator commits after independent verification.
