# V0.51 working-hands loop (self-contained)

Read `V0.51_WORKING_HANDS_SPEC.md` in this directory first — it is the
authoritative spec; this file is the loop contract.

Repo: `/Users/nathanmaxwell/Documents/VS Code/games-room` (workdir). Branch: main.
Do NOT create a branch. Do NOT commit.

## Task

Add a per-member working-hand readout for active casino play: host sees every
member's working hand + bank balance plus the to-dispense zone; members see
working hands only. New RPC + WorkingHand model + ChipsOnTableSection UI.
Remove the standalone HostWithdrawalsSection block and merge dispense into the
new section.

## Hard constraints

1. **Exactly these files may change:**
   - NEW `Supabase/migrations/065_event_working_hands.sql`
   - NEW `GamesRoom/Models/WorkingHand.swift`
   - `GamesRoom/Services/CasinoService.swift` (add `loadWorkingHands(eventId:)`)
   - `GamesRoom/Views/RoomDetailView.swift` (state, loader, refresh wiring,
     ChipsOnTableSection, remove standalone dispense block)
   - `GamesRoom.xcodeproj/project.pbxproj` (register WorkingHand.swift ONLY)
   - `main.swift` (add WorkingHand round-trip test)
   - `docs/loop-artifacts/V0.51_WORKING_HANDS_SPEC.md` (only to record deviations)
   - `docs/loop-artifacts/.v0.51_working_hands_loop.md` (this file)
   Do NOT touch any other file. There are untracked `.designs/` and `build/`
   dirs — never `git add -A`, never commit anything outside this list.
2. **New files must be registered in pbxproj** exactly like the existing
   Models pattern (copy the `CasinoWithdrawal.swift` PBXFileReference +
   PBXBuildFile + group entry + Sources entry, using the next sequential
   24-hex ids already in the file — reuse `AAAAAAAA0000000000000427` /
   `AAAAAAAA0000000000000428` / `AAAAAAAA00000000000000C5` if free, else
   match the file's id convention). `verify-xcode-project.py` must pass.
3. **Migration style:** match 061/040 — `security definer`,
   `set search_path = public`, `grant execute ... to authenticated`,
   `notify pgrst, 'reload schema';` at the end. No migration renumbering.
4. **WorkingHand** is a NEW `GamesRoom/Models/WorkingHand.swift` (Foundation
   only, Codable + Identifiable + Hashable, snake_case CodingKeys, memberwise
   init). Do NOT put it in CasinoService.swift — the test build globs
   `GamesRoom/Models/*.swift`.
5. **Section render gate:** `isCasinoPlayActive` (packSlug casino, playedAt
   <= now, settledAt == nil) AND (any hand.workingHand > 0 OR pending
   withdrawals non-empty). Hide otherwise.
6. **Balance visibility:** `pointsBalance` shown ONLY when `isHost`, as a
   trailing muted column. Members never see balances.
7. **Remove** the standalone `HostWithdrawalsSection` block at
   RoomDetailView.swift lines 406-415 from scrollBody; keep the
   `HostWithdrawalsSection` struct as the dispense sub-zone inside
   `ChipsOnTableSection`. Wire `onDispense` to `dispensedWithdrawalIds.insert`.
8. **refresh():** add `async let workingHandsLoad: () = loadWorkingHandsIfNeeded()`
   after the `hostWithdrawalsLoad` line (~745). No new polling.
9. Match each file's existing conventions (Theme.Typography, Theme.Palette,
   `.sectionCard(.standard)`, Theme.Icon). No dead code, no TODOs, no comments
   that restate the code.

## Deliverables

1. Migration 065 defines `get_event_working_hands(p_event_id)` per SPEC Change 1.
2. `WorkingHand` model + `CasinoService.loadWorkingHands(eventId:)` non-throwing.
3. `ChipsOnTableSection` in scrollBody between activeSlot and standings;
   standalone HostWithdrawalsSection block removed.
4. Host sees working hands + bank balances + to-dispense zone; members see
   hands only; section hides when empty.
5. Dispense acknowledge still works.
6. main.swift WorkingHand round-trip test added.

## Verification (mandatory, in order)

1. `./build-and-run-tests.sh` from repo root — all green. Report the test count
   (expect 160) and parse-check file count (expect 45).
2. `python3 scripts/verify-xcode-project.py` — green.
3. `git diff --stat` — exactly the files above.
4. Do NOT commit. Report: files changed, test count, parse-check result,
   verify-xcode-project result, any deviation from the SPEC.
